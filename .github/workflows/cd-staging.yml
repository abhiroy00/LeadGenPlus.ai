# Temporarily disabled until server is ready
# - name: Deploy to staging server
#   uses: appleboy/ssh-action@v1
#   with:
#     host: ${{ secrets.STAGING_SERVER_IP }}
#     ...
```

**Option B — Add fake secrets so workflow doesn't crash**
```
In GitHub Secrets add:
  STAGING_SERVER_IP  →  0.0.0.0  (placeholder)
  STAGING_SSH_KEY    →  placeholder
  PROD_SERVER_IP     →  0.0.0.0
  PROD_SSH_KEY       →  placeholder
  SLACK_WEBHOOK_URL  →  https://placeholder.com
  VITE_API_URL       →  http://localhost:8000/api
```

---

## What to do RIGHT NOW

Tell me:
```
1. Have you created a Docker Hub account?
   Yes / No

2. Do you have an EC2 server running?
   Yes / No